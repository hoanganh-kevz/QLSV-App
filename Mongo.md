# Bước 1: Tạo project mới
# Bước 2: Build Cluster
# Bước 3: Nhập MÔNG URI vào file .env
# BƯớc 4: nếu bị lỗi thì dùng dns cấu hình lại 

# Cách xem data: Vào data explorer -> chọn connect -> Chọn Cluster0 -> Chọn test -> chọn users

# Lỗi mỗi ngày wifi reset IP: Nhân vào IP ACCESS LIST 0.0.0.0/0
CONNECT -> sửa lại mongo_URI

